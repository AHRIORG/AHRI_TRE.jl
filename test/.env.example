# Optional test-only environment overrides
# Copy to test/.env (kept out of git by *.env) to override /workspace/.env values during `Pkg.test()`.
#
# Example:
# MSQLServer=localhost
# MSQLServerDB=AHRI-TRE-Test
# MSQLServerUser=sa
# MSQLServerPW=YourStrong!Passw0rd
#
# You can also override ODBC driver path if needed:
# ODBC_DRIVER_PATH=/opt/microsoft/msodbcsql18/lib64/libmsodbcsql-18.5.so.1.1
